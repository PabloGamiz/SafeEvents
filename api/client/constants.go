package client

const (
	// APISigninPath is the path where all signin request will be handled
	APISigninPath = "/signin"
	// APILogoutPath is the path where all logout request will be handled
	APILogoutPath = "/logout"
	//APIClientInfoPath is the path where all the client info request will be handled
	APIClientInfoPath = "/clientinfo"
	//APIAddFavPath is the path where all the add fav request will be handled
	APIAddFavPath = "/addfav"
	//APIDelFavPath is the path where all the delete fav request will be handled
	APIDelFavPath = "/delfav"
	//APIStatusPath is the path where all the delete fav request will be handled
	APIStatusPath = "/status"
)
