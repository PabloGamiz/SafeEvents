package radar

const (
	// APIActivatePath is the path where all activation requests will be handled
	APIActivatePath = "/radar/activate"
	// APIInteractionPath is the path where all interactions requests will be handled
	APIInteractionPath = "/radar/interaction"
)
